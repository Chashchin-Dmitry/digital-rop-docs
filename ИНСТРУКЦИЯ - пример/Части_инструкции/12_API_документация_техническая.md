# Техническая документация API

**Раздел для разработчиков и системных интеграторов**

Система "Цифровой РОП" предоставляет REST API для интеграции с внешними системами. Данный раздел содержит полную техническую документацию для настройки интеграции.

## 🔗 Общие принципы работы API

### Базовый URL
```
http://10.28.32.81/api/v1/
```

### Аутентификация
{TECHNICAL} Все запросы к API должны содержать параметр `apiKey` с ключом доступа. Для Setl Group используется ключ:
```
LS6gz1bkpG3lZI6uXetEoLtoVtVXYuWO
```

### HTTP методы
- **POST** - для отправки данных в систему
- **GET** - для получения информации  
- **Webhook** - для автоматической отправки результатов

## 📥 Входящие методы API (CRM → Цифровой РОП)

### 1. Загрузка аудиозаписей

**Endpoint:**
```
POST http://10.28.32.81/api/v1/calls/audio-upload?apiKey={API_KEY}
```

**Описание:** Отправка ссылки на аудиофайл для анализа

**Параметры запроса:**
```json
{
  "manager_id": "string",      // ID менеджера из CRM
  "call_id": "string",         // Уникальный ID звонка  
  "deal_id": "string",         // ID сделки в CRM
  "deal_stage_id": "string",   // ID этапа сделки
  "audio_url": "string",       // URL для скачивания аудио
  "duration": "integer"        // Длительность в секундах (опционально)
}
```

**Пример cURL запроса:**
```bash
curl --location --request POST 'http://10.28.32.81/api/v1/calls/audio-upload?apiKey=LS6gz1bkpG3lZI6uXetEoLtoVtVXYuWO' \
--form 'manager_id="mgr_001"' \
--form 'call_id="call_12345_20250619"' \
--form 'deal_id="deal_5678"' \
--form 'deal_stage_id="stage_qualification"' \
--form 'audio_url="https://crm.company.com/files/audio/call_12345.mp3"'
```

**Альтернативный JSON формат:**
```bash
curl --location --request POST 'http://10.28.32.81/api/v1/calls?apiKey=LS6gz1bkpG3lZI6uXetEoLtoVtVXYuWO' \
--header 'Content-Type: application/json' \
--data-raw '{
  "call_id": "call_12345_20250619",
  "audio_url": "https://crm.company.com/files/audio/call_12345.mp3", 
  "duration": 900,
  "manager_id": "mgr_001",
  "deal_id": "deal_5678",
  "deal_stage_id": "stage_qualification"
}'
```

**Ответы:**
- **200 OK**: `{"data":"call_id","error":false,"message":"Звонок успешно получен и поставлен в очередь на анализ"}`
- **400 Bad Request**: `{"data":false,"error":true,"message":"Файл не является аудио"}`
- **401 Unauthorized**: `{"data":false,"error":true,"message":"apiKey не валиден"}`
- **409 Conflict**: `{"data":false,"error":true,"message":"Звонок с данным call_id уже обработан"}`

### 2. Обновление структуры офисов

**Endpoint:**
```
POST http://10.28.32.81/api/v1/settings/offices-update?apiKey={API_KEY}
```

**Описание:** Синхронизация структуры офисов и сотрудников

**Параметры запроса:**
```json
{
  "offices": [
    {
      "office_title": "string",        // Название офиса
      "office_id": "string",           // ID офиса  
      "leader_ids": ["uuid1", "uuid2"], // Массив ID руководителей
      "managers_ids": ["uuid3", "uuid4"] // Массив ID менеджеров
    }
  ]
}
```

**Пример запроса:**
```json
{
  "offices": [
    {
      "office_title": "Офис продаж Центр",
      "office_id": "office_001", 
      "leader_ids": ["leader_123", "leader_456"],
      "managers_ids": ["manager_789", "manager_012", "manager_345"]
    },
    {
      "office_title": "Офис продаж Север",
      "office_id": "office_002",
      "leader_ids": ["leader_789"],
      "managers_ids": ["manager_111", "manager_222"]
    }
  ]
}
```

### 3. Обновление данных менеджеров

**Endpoint:**  
```
POST http://10.28.32.81/api/v1/settings/managers-update?apiKey={API_KEY}
```

**Описание:** Синхронизация информации о менеджерах

**Параметры запроса:**
```json
{
  "managers": [
    {
      "manager_id": "string",  // ID менеджера
      "name": "string",        // Имя
      "surname": "string",     // Фамилия  
      "middlename": "string"   // Отчество
    }
  ]
}
```

**Пример запроса:**
```json
{
  "managers": [
    {
      "manager_id": "manager_789",
      "name": "Иван", 
      "surname": "Иванов",
      "middlename": "Иванович"
    },
    {
      "manager_id": "manager_012",
      "name": "Мария",
      "surname": "Петрова", 
      "middlename": "Сергеевна"
    }
  ]
}
```

### 4. Проверка готовности результатов таблицы

**Endpoint:**
```
GET http://10.28.32.81/api/v1/tables/call-ready?tab={TABLE_ID}&call={CALL_ID}
```

**Описание:** Проверка готовности результатов аналитической таблицы

**Параметры:**
- `tab` - идентификатор таблицы (например: TBL_1755863365)
- `call` - идентификатор звонка

**Пример запроса:**
```
GET http://10.28.32.81/api/v1/tables/call-ready?tab=TBL_1755863365&call=8
```

**Ответ при готовности:**
```json
{
  "ready": true,
  "data": {
    "field1": "значение1", 
    "field2": "значение2",
    "custom_field": "результат анализа"
  }
}
```

## 📤 Исходящие Webhook (Цифровой РОП → CRM)

### 1. Результаты проверки по чек-листу

**Описание:** Автоматическая отправка результатов анализа звонка по корпоративному скрипту

**Настройка:** URL webhook настраивается в разделе "Настройки → Скрипты и промты" для каждого чек-листа отдельно

**Формат данных:**
```json
{
  "call_id": "call_789456",
  "manager_id": "12345", 
  "checklist_name": "Чек-лист (тест 001)",
  "total_score": 18,
  "max_score": 20,
  "completion_percentage": 90,
  "stages": [
    {
      "stage_id": "URC_S_1755863272",
      "stage_name": "МРК выяснил все критерии поиска клиента",
      "score": 1,
      "percentage": 85,
      "status": "completed",
      "comment": "Менеджер уточнил количество комнат, район и сроки сдачи",
      "quote": "Какое количество комнат вас интересует и в каком районе?"
    },
    {
      "stage_id": "URC_S_1755863663", 
      "stage_name": "МРК выяснил финансовую ситуацию клиента",
      "score": -1,
      "percentage": 15,
      "status": "failed", 
      "comment": "Менеджер не выяснил размер бюджета и возможности по ипотеке",
      "quote": ""
    }
  ],
  "timestamp": "2025-09-17T14:30:00Z"
}
```

### 2. Результаты аналитических таблиц

**Описание:** Отправка результатов кастомного анализа по настроенным таблицам

**Настройка:** URL webhook настраивается индивидуально для каждой таблицы в разделе "Настройки → Таблицы"

**Формат данных:**
```json
{
  "call_id": "call_789456",
  "table_id": "table_conversion", 
  "table_name": "Анализ конверсии",
  "extracted_data": {
    "client_name": "Иван Петров",
    "budget_range": "5-7 млн руб",
    "object_type": "квартира", 
    "conversion_stage": "заинтересован",
    "objections": "дорого; нужно подумать",
    "next_action": "отправить расчет",
    "confidence_level": "высокая"
  },
  "processing_time": "00:02:35",
  "timestamp": "2025-09-17T14:32:35Z"
}
```

## 📊 Коды ответов HTTP

| Код | Описание | Применение |
|-----|----------|------------|
| 200 | OK | Успешная обработка запроса |
| 400 | Bad Request | Некорректные параметры запроса |
| 401 | Unauthorized | Неверный или отсутствующий API ключ |
| 404 | Not Found | Запрашиваемый ресурс не найден |
| 409 | Conflict | Дублирование данных (например, повторный call_id) |
| 500 | Internal Server Error | Внутренняя ошибка сервера |

## 🔒 Безопасность и ограничения

### Ограничения API
- **Максимальный размер аудиофайла:** 100 МБ
- **Поддерживаемые форматы:** MP3, WAV, OGG  
- **Максимальная длительность:** 45 минут (2700 секунд)
- **Частота запросов:** не более 60 запросов в минуту

### Рекомендации по безопасности
- Храните API ключ в безопасном месте
- Используйте HTTPS для передачи данных в продакшене
- Логируйте все API запросы для отладки
- Реализуйте повторные попытки при ошибках 5xx
- Проверяйте целостность данных перед отправкой

## 🛠️ Примеры интеграции

### Python пример
```python
import requests
import json

# Конфигурация
API_KEY = "LS6gz1bkpG3lZI6uXetEoLtoVtVXYuWO"
BASE_URL = "http://10.28.32.81/api/v1"

def send_call_for_analysis(call_data):
    """Отправка звонка на анализ"""
    url = f"{BASE_URL}/calls/audio-upload?apiKey={API_KEY}"
    
    response = requests.post(url, json=call_data)
    
    if response.status_code == 200:
        print("Звонок успешно отправлен на анализ")
        return response.json()
    else:
        print(f"Ошибка: {response.status_code} - {response.text}")
        return None

def check_table_ready(table_id, call_id):
    """Проверка готовности результатов таблицы"""
    url = f"{BASE_URL}/tables/call-ready?tab={table_id}&call={call_id}"
    
    response = requests.get(url)
    if response.status_code == 200:
        data = response.json()
        return data.get("ready", False), data.get("data", {})
    return False, {}

# Использование
call_data = {
    "manager_id": "mgr_001",
    "call_id": "call_12345",
    "deal_id": "deal_5678", 
    "audio_url": "https://crm.example.com/audio/call_12345.mp3"
}

result = send_call_for_analysis(call_data)
```

### PHP пример
```php
<?php
class DigitalROPAPI {
    private $apiKey = 'LS6gz1bkpG3lZI6uXetEoLtoVtVXYuWO';
    private $baseUrl = 'http://10.28.32.81/api/v1';
    
    public function sendCall($callData) {
        $url = $this->baseUrl . '/calls/audio-upload?apiKey=' . $this->apiKey;
        
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $url);
        curl_setopt($ch, CURLOPT_POST, 1);
        curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($callData));
        curl_setopt($ch, CURLOPT_HTTPHEADER, [
            'Content-Type: application/json'
        ]);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        
        $response = curl_exec($ch);
        $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);
        
        return ['code' => $httpCode, 'data' => json_decode($response, true)];
    }
    
    public function updateOffices($offices) {
        $url = $this->baseUrl . '/settings/offices-update?apiKey=' . $this->apiKey;
        
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $url);
        curl_setopt($ch, CURLOPT_POST, 1);
        curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode(['offices' => $offices]));
        curl_setopt($ch, CURLOPT_HTTPHEADER, [
            'Content-Type: application/json'
        ]);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        
        $response = curl_exec($ch);
        $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);
        
        return ['code' => $httpCode, 'data' => json_decode($response, true)];
    }
}

// Использование
$api = new DigitalROPAPI();

$result = $api->sendCall([
    'manager_id' => 'mgr_001',
    'call_id' => 'call_12345', 
    'deal_id' => 'deal_5678',
    'audio_url' => 'https://crm.example.com/audio/call_12345.mp3'
]);

echo "Результат: " . $result['code'] . "\n";
print_r($result['data']);
?>
```

## 🔄 Процесс интеграции пошагово

### Этап 1: Подготовка
1. Получите API ключ у администратора системы
2. Убедитесь в доступности URL `http://10.28.32.81` из вашей сети
3. Подготовьте тестовые аудиофайлы для проверки

### Этап 2: Настройка отправки звонков
1. Реализуйте отправку POST запросов на `/calls/audio-upload`
2. Обработайте возможные ошибки и коды ответов
3. Протестируйте на тестовых данных

### Этап 3: Настройка получения результатов  
1. Создайте endpoint в вашей CRM для получения webhook
2. Настройте URL webhook в интерфейсе "Цифровой РОП"
3. Реализуйте обработку входящих результатов

### Этап 4: Синхронизация данных
1. Настройте отправку структуры офисов через `/settings/offices-update`  
2. Настройте синхронизацию менеджеров через `/settings/managers-update`
3. Протестируйте корректность отображения в системе

### Этап 5: Продакшн
1. Переведите интеграцию в боевой режим
2. Настройте мониторинг и логирование
3. Реализуйте обработку ошибок и повторные попытки

## 📞 Техническая поддержка

При возникновении проблем с интеграцией:

1. **Проверьте базовые параметры:**
   - Корректность API ключа
   - Доступность URL из вашей сети
   - Формат передаваемых данных

2. **Логирование:**
   - Записывайте все запросы и ответы
   - Проверяйте коды HTTP ответов
   - Анализируйте сообщения об ошибках

3. **Отладка:**
   - Тестируйте с помощью curl или Postman
   - Проверьте формат JSON данных
   - Убедитесь в корректности URL аудиофайлов

Данная документация покрывает все аспекты технической интеграции с системой "Цифровой РОП" и должна обеспечить успешное подключение внешних систем.